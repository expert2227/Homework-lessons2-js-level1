var a = 1, b = 1, c, d;
c = ++a; alert(c);        // 2 потому что переменной a присваивается оператор инкремент, причем он сразу проводится, и тут же выводится готовое увеличенное значение. 
d = b++; alert(d);        // 1  выводится потому-что икремент только проводится, если вызвать его еще раз то он покажет результат с увеличенным значением на единицу. 
c = (2+ ++a); alert(c);   // 5 в этой строке кода с пепеменной а опять проводится оператор инкремент причем он тут же выдается в этом значении, получается что у переменной а было значение 2, к которому мы прибавили 1, и получили 3. Тут же прибавили 2 и в итоге сумма значения равна 5.
d = (2+ b++); alert(d);   // 4 получилась потому что инкремент ещё не выведен, но уже проведён, мы видим результат оператора из предыдущего действия с этой переменной.
alert(a);                 // 3 если в целом посмотреть на все строки кода то мы увидим что с переменной а было выполнено 2 операции (инкремент) и наш пример прекрасно отображает оба варианта постфиксного и префиксноно инкремента
alert(b);                 // 3 с этой переменной абсолютно тоже самое.
